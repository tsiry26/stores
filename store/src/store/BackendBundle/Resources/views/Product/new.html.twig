{% extends "storeBackendBundle::layout.html.twig" %}

{#Cette fonction va me chercher un thème de formulaire#}
{% form_theme form 'storeBackendBundle:Partial:form.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    <script>
        init.push(function () {
            if (! $('html').hasClass('ie8')) {
                $('textarea').summernote({
                    height: 200,
                    tabsize: 2,
                    codemirror: {
                        theme: 'monokai'
                    }
                });
            }
        });
    </script>
{% endblock %}
{% block titlepage %}
    <i class="fa fa-plus"></i> Creation de produit
{% endblock %}

{% block content %}
        {{ include('storeBackendBundle:Product:_form.html.twig') }}
{% endblock %}